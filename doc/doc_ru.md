# TUI_DB 🚀

Я — студент и начинающий C++ разработчик.
Мой проект TUI_DB родился из простой идеи: создать максимально легкий и быстрый инструмент для работы с базами данных (SQLite и PostgreSQL).

## 🎯 Зачем это нужно?

Когда вы разрабатываете API или отлаживаете приложение, вам часто нужно «взглянуть» на данные прямо здесь и сейчас.
TUI_DB позволяет подключиться к базе в кратчайшие сроки с минимумом настроек.

## 🖥 Почему TUI?

Приложение работает непосредственно внутри терминала, что делает его идеальным для SSH-сессий.
В отличие от тяжелых GUI-клиентов, вам не нужно настраивать сложные SSH-туннели или пробрасывать порты базы данных на локальную машину.
Достаточно запустить бинарник прямо на сервере и мгновенно получить удобный интерфейс.

---

## 🛠 Архитектура и компоненты

Приложение построено на базе современных стандартов C++23 и нескольких ключевых компонентов:

1. LoginForm: отвечает за авторизацию и выбор типа БД.
2. DataBaseExplorer: основной узел для обработки SQL-запросов и их визуализации.
3. Data Access Layer: специализированные классы для взаимодействия с конкретными СУБД.
4. Обработка ошибок: Если вы ошиблись в синтаксисе, приложение выведет сообщение об ошибке прямо в интерфейс в режиме реального времени.

## 🎮 Навигация и управление

- Перемещение: используйте стрелки [↑] и [↓] для перехода между элементами интерфейса.
- Выполнение: введите запрос, переключитесь на кнопку "Enter" и нажмите клавишу ввода.
- Прокрутка: нижние прогресс-бары позволяют пролистывать таблицу. Когда фокус на них, используйте стрелки [←] и [→].

## ⚡ Оптимизация и скорость

Чтобы интерфейс не «зависал» при получении тысяч строк, я реализовал постраничный просмотр:

- На одной странице отображается фиксированно 500 записей (параметр rows_per_page в коде, который вы можете легко изменить под себя).
- Переключение страниц осуществляется клавишами [PgUp] и [PgDown].

---

## 📦 Технический стек

- Стандарт: C++23
- Сборка: CMake
- Зависимости: vcpkg (FTXUI, SQLite3, libpq, libpqxx)

Я понимаю, что проект еще в начале пути и не идеален, но это мой первый серьезный вклад в Open Source и отличная практика в системном дизайне.
Если у вас есть идеи или вы нашли баг — жду вас в разделе Discussions!
